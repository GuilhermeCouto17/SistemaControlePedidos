#ifndef VALIDACOES_H
#define VALIDACOES_H

int validarCPF(const char *cpf);        //Valida o cpf
int validarCNPJ(const char *cnpj);      //Valida o cnpj
int validarTelefone(const char *tel);       //Valida o numero de telefone da pessoa 
int validarEmail(const char *email);        //Valida o email da pessoa
int validarTexto(const char *s);        //Verifica se o texto é valido antes de aceitar o cadastro da pessoa 

#endif

//Aqui é todas as funções que usaram na vaidação e para que elas servem 